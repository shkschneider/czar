# 0 - C-like

## DONE

- statically linked
- standard library
- extended library

## TODO

- unused, file, line, function, unreachable (throws)...
- not eq lt le gt ge ls rs and or -> ! == < <= > >= << >> && ||
- any -> void
- foreach
- bit byte, i8 i16 i32 i64, u8 u16 u32 u64, f8 f16 f32 f64
- if cond {} -> if (cond) {}
- myalias :: u8 -> typedef u8 myalias
- main :: function (...) int -> int main(...)
- mystruct :: struct {} -> typedef struct mystruct {}
- myenum :: enum {} -> enum myenum {}
- import("whatever", param...) -> import "whatever" + whatever.init(param...)
- string (sized, not 0-terminated)
- helper library: string, defer...
- defer: return -> _defer(); return;
- array ([...])
