#module cz

pub struct Version {
    u32 major
    u32 minor
    u32 patch
}

fn Version:tostring() cstr {
    u32 maj = self.major
    u32 min = self.minor
    u32 pat = self.patch
    return "{maj}.{min}.{pat}"
}
