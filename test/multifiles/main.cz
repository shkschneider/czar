// Test file: main function that uses functions from same directory
// According to new requirements, files in same folder should work without #import

int main(void) {
    i32 result = add(10, 20);
    return 0;
}
