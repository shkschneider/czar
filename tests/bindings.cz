// Test variable bindings: let (immutable) and mut (mutable)
fn main() -> i32 {
    let x: i32 = 10      // immutable
    mut y: i32 = 20      // mutable
    y = y + x
    return y             // should return 30
}
