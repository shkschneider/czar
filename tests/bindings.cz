// Test variable bindings: val (immutable) and var (mutable)
fn main() -> i32 {
    val i32 x = 10      // immutable
    var i32 y = 20      // mutable
    y = y + x
    return y             // should return 30
}
