// Test compound assignment operators with pointers
struct Point {
    mut i32 x
    mut i32 y
}

fn main() -> i32 {
    mut Point p = Point { x: 10, y: 20 }
    Point ptr = p

    // Test compound assignments through pointer
    ptr.x += 5   // p.x should be 15
    ptr.y *= 2   // p.y should be 40

    return ptr.x + ptr.y  // should return 55
}
