// Test that & operator no longer exists (removed in nullable safety update)
// This test should fail at parse/compile time
fn get_value() i32 {
    return 42
}

fn main() i32 {
    // ERROR: & operator no longer supported
    // In the new model, all types are implicitly pass-by-reference
    i32 x = get_value()
    return x
}
