// Test that mut item requires mut array
fn main() i32 {
    i32[3] arr = [ 10, 20, 30 ]  // immutable array
    
    // ERROR: cannot have mut item with immutable array
    for _, mut item in arr {
        item = 42
    }
    
    return 0
}
