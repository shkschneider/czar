// FAIL TEST: String comparison with == should not work directly
fn main() i32 {
    string s1 = string "hello"
    string s2 = string "hello"
    
    // This should fail - no == operator for string values
    // (Would compare struct addresses, not content)
    if s1 == s2 {
        return 0
    }
    
    return 1
}
