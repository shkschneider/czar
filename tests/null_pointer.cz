// Test null pointer literal
struct Node {
    i32 value
}

fn main() -> i32 {
    mut Node p = null

    // Check if we can assign null and non-null
    if p == null {
    Node n = Node { value: 42 }
        p = n
        return p.value
    }

    return 0
}
