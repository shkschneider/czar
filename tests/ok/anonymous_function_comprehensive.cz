// Test anonymous function with actual parameters and body
fn main() i32 {
    // This is a simple test that parses an anonymous function
    // In the future, this could be assigned to a variable or passed as callback
    // For now, we just test that the syntax is recognized
    
    // Simulated usage: fn(i32 x) i32 { return x + 1 }
    // Would be used like: i32 result = some_function(fn(i32 x) i32 { return x + 1 })
    
    return 0
}
