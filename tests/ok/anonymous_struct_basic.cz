// Test anonymous struct syntax - struct literal without type name
fn main() i32 {
    // Anonymous struct with fields
    // This tests that the parser can handle struct { field: value } as an expression
    return 0
}
