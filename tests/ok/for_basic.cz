// Test basic for loop with immutable array
fn main() i32 {
    i32[3] arr = [ 10, 20, 30 ]
    mut i32 sum = 0
    
    for idx, val in arr {
        sum = sum + val
    }
    
    return sum - 60  // 10 + 20 + 30 = 60
}
