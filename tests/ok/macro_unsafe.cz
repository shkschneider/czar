// Test #unsafe block
// Verifies that raw C code can be embedded and passed through

fn test_unsafe() i32 {
    mut i32 x = 0
    
    #unsafe {
        x = 42;
        printf("Raw C code: %d\n", x);
    }
    
    return 0
}

fn main() i32 {
    return test_unsafe()
}
