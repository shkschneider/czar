// Test string:find() method
fn main() i32 {
    String s = String { "Hello World" }
    String needle = String { "World" }
    
    // Find substring
    i32 index = s.find(needle)
    
    if index != 6 {
        return 1
    }
    
    // Test not found
    String missing = String { "xyz" }
    i32 not_found = s.find(missing)
    
    if not_found != -1 {
        return 2
    }
    
    return 0
}
