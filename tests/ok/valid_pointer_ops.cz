// Test that valid pointer operations are still allowed
fn main() i32 {
    mut i32 x = 10
    i32? p = &x
    
    // Dereferencing is allowed
    i32 y = *p
    
    // Pointer comparison is allowed
    bool same = p == &x
    
    return y - 10
}
